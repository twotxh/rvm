using System;

namespace RobinScript
{
    class Parser
    {
        public static Bytecode Parse(Lexer lexedCode)
        {
            Bytecode BTTable = new Bytecode();
            for (int index = 0; index < lexedCode.Count; index++) {

            }
            return BTTable;
        }
    }
}
